#ifndef ENUMS_HPP
#define ENUMS_HPP

namespace globalEnums
{
  enum Player
  {
    HUMAN = 0,
    COMPUTER = 1
  };

  enum CurrentState
  {
    UNIT_REDEPLOYMENT,
    MANDATORY_ACTION
  };
}

#endif
